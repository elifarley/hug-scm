# ADL — Autonomous Decision Log — 2026-08-15

Autonomous agent decisions with reasoning and sources.
Scope: choices made without explicit user direction. User-driven -> DEC-NNN (/kanban-ops).

<!-- Entries: append below with UTC-3 timestamp -->

## 08:09 — PR 277 review: @{-1} guard-arm disposition

**Context**: pr-fix on elifarley/hug-scm#277: codex-connector P2 claimed @{-1}/@{-2} resolve in orphan checkouts so the unborn-HEAD guard's broad @* arm mis-brands them.
**Decision**: Re-probed on the floor git 2.34.1 before acting: @{-1} does NOT resolve while HEAD is unborn (it reads the HEAD reflog, which does not exist — rev-parse exit 128), so the reviewer premise was refuted. Still adopted the arm refinement @ | @~* | @^* | '@{'[0-9]* because the conclusion holds under the spec D5 contract: an unresolvable @{-1} is an invalid explicit ref and must keep git's raw exit-128 fatal, which the broad @* arm would have branded. Applied to BOTH the plan and the spec in the same commit to prevent the drift class four roast rounds existed to prevent.
**Alternatives**: Push back fully on the refuted premise and keep the broad @* arm (rejected: it violates D5 for unresolvable @-forms); pre-resolve every ref via rev-parse before branding (rejected: changes invalid-ref behavior in unborn repos away from D5's raw-fatal contract).
**Sources**: https://git-scm.com/docs/githooks -- not used; primary evidence: local probes on git 2.34.1 (git rev-parse @{-1} after git switch --orphan; git reflog show HEAD) per the repo's probe discipline (spec 2026-08-14-shc-deferred-follow-ups, git floor 2.34)

## 15:14 — CI sanitize-check red on PR 277: scope of the fix under tool-version drift

**Context**: pr-fix on elifarley/hug-scm#277: CI sanitize-check failed (SC2314 error-severity, tests/unit/test_sh.bats:808) while local sanitize-check was green — local shellcheck 0.8.0 predates the check, CI installs the latest unpinned release. Same log showed pre-existing warning/info findings (SC1007/SC2016/SC2209/SC2295) in files the PR never touched.
**Decision**: Fixed ONLY the error-severity finding in a PR-touched file (bare ! probe → run ! + assert_failure + bats_require_minimum_version 1.5.0, per house precedent at test_hug-git-commit.bats:94); left the pre-existing warnings untouched — the repo gate fails on error-severity only, and widening a CI-hotfix push to churn out-of-diff files trades a merge blocker for review noise. Filed the root cause (unpinned shellcheck drift) as elifarley/hug-scm#288 rather than fixing it inside this PR (infra change, independently shippable). Disposition note: bats run ! preserves the command's REAL exit code in $status (the ! only inverts run's pass criterion) — learned empirically when assert_success failed on status 1; the companion assert must be assert_failure.
**Alternatives**: Fix all 9 findings incl. pre-existing warnings in one push (rejected: out-of-diff churn on a triple-reviewed PR, gate only demands error-severity); pin shellcheck in this PR (rejected: infra scope-creep on a feature PR — tracked instead); leave bare ! with a shellcheck disable directive (rejected: run ! is strictly stronger and is the linter's own suggested form).
**Sources**: https://www.shellcheck.net/wiki/SC2314
**References**: elifarley/hug-scm#288 elifarley/hug-scm#277 elifarley/llm-prompting#263

## 15:21 — CI test(lib) red: hermetic-fixture verification under identity-stripped env

**Context**: Second CI failure on elifarley/hug-scm#277 (after 0d192d0 unblocked sanitize-check): the range-branch submodule matrix test failed in CI with 'Author identity unknown' — the fixture commits inside the submodule worktree, a separate repo (.git/modules/sub) that inherits neither the parent's local identity nor a global one (runners have none). Locally green via the developer's global identity; the single-branch twin passes because it never commits inside sub/. The lib job had never completed before on this PR (fail-fast cancels), so the defect was invisible until now.
**Decision**: Fixed by configuring user.email/user.name inside sub/ before the childchange commit (same pattern the fixture already used for the child source repo). Verified under simulated CI conditions — GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null — before pushing, rather than trusting the local green run that had already lied twice this session (sanitize-check SC2314, then this). The identity-stripped env run is the cheap, transferable check for any test that touches nested repos.
**Alternatives**: Commit with -c user.email/-c user.name one-shot flags (rejected: three flags per commit vs two config lines; the config form matches the fixture's existing child-repo pattern); rely on CI to verify (rejected: a push-burn cycle costs ~15 min and this session already had two local-green-CI-red misses).
**Sources**: https://git-scm.com/docs/git-config#Documentation/git-config.txt-username
**References**: elifarley/hug-scm#277 elifarley/hug-scm#286 elifarley/hug-scm#288

## 16:19 — PR 277 CI: git-2.54 origin/HEAD symref fixture collapse

**Context**: pr-fix on elifarley/hug-scm#277: the endpoint-verification test (range with resolving HEAD-containing endpoints) was red on CI (git 2.54) while green on the declared floor (2.34.1). Two commits chased symptoms (remote-ref ~1 traversal, then the non-vacuousness gate where origin/HEAD and origin/master collapsed to the same SHA).
**Decision**: Root cause: git 2.54's  opportunistically creates an  symref -> origin/master, and  follows the surviving symref (the preceding  does not reliably kill it on 2.54) instead of repinning a plain ref, collapsing both endpoints. The guard's  arm is substring-based and name-agnostic, so I replaced the remote-symref fixture with a LOCAL branch named  (git update-ref refs/heads/A-HEAD master~1) + . The range  exercises the identical range-rescue path with no remote/fetch/symref/protocol. The single-ref  rescue test (which passes on CI) keeps remote-ref coverage. Verified: CI green on 026fe27.
**Alternatives**: Keep fighting the origin/HEAD symref (symbolic-ref -d, or pin via a non-following path) — rejected: the fixture tests substring guard logic, not remote-ref semantics; the single-ref test already covers the remote case. Drop the range-rescue test entirely — rejected: it guards a real over-fire regression (HEAD-containing ranges with resolving endpoints).
**Sources**: https://git-scm.com/docs/git-symbolic-ref (symref semantics); local probes on git 2.34.1 (floor) confirming A-HEAD..master exercises the same *HEAD* guard arm; CI run 31902379234 (git 2.54.0) failure receipts
