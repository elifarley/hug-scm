# ADL — Autonomous Decision Log — 2026-08-25

Autonomous agent decisions with reasoning and sources.
Scope: choices made without explicit user direction. User-driven -> DEC-NNN (/kanban-ops).

<!-- Entries: append below with UTC-3 timestamp -->

## 22:08 — NUL transport + root-relative probe for canonicalize_source_lines (plan doc)

**Context**: PR elifarley/hug-scm#318 codex review flagged three P1 defects in the implementation plan sketches: bash command substitution strips NUL bytes from git ls-files -z output; membership lookup compared CWD-relative literals against a root-relative output set; Task 5 dropped the in_scope intersection filter.
**Decision**: Stream NUL output through a temp FILE instead of $(...) (also preserves exit status, keeping failures loud); lift each input line to its root-relative spelling before the seen[] probe and push the root spelling resolved; restore the O(n+m) assoc-filter intersection verbatim. Also collapsed the LITERAL/FREE shape classifier into membership-hit fast path vs per-line fallback on ANY miss — dir/glob fan-out and loud-failure semantics become provably byte-identical, and a poisoned batch degrades to per-line behavior.
**Alternatives**: mapfile -d (bash >=4.4 only; zero precedent in repo); printf %s "$batch_out" re-split (impossible: NULs already stripped by substitution); keeping the shape classifier (two code paths to keep byte-identical, more surface).
**Sources**: ⚠️ TODO — SOURCES MISSING (web-search guidance before deciding, then edit this entry)
**References**: PR#318
