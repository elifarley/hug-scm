# ADL — Autonomous Decision Log — 2026-09-03

Autonomous agent decisions with reasoning and sources.
Scope: choices made without explicit user direction. User-driven -> DEC-NNN (/kanban-ops).

<!-- Entries: append below with UTC-3 timestamp -->

## 20:51 — Issue 281 scope: help-text-only xargs -r fix, docs page synced too

**Context**: Executing elifarley/hug-scm#281 (add -r to the xargs -0 call at git-config/bin/git-s:84). Direct read of git-s on origin/main (292bbb36) showed line 84 is inside the show_help() heredoc (CAPTURING OUTPUT section), NOT live code; a repo-wide grep found no live xargs -0 anywhere in git-config/ or hg-config/. Mid-pipeline, the code-quality review surfaced that docs/commands/status-staging.md:82 carried a byte-identical copy of the same unsafe example.
**Decision**: Scoped the fix as help-text-only (no live-code change, so the issue's both-cases runtime unit test was adapted to a two-directional grep assertion on hug s: Quick summary of working tree state and staged changes

USAGE:
  hug s [OPTIONS]

OPTIONS:
  -h, --help       Show this help message and exit.

OUTPUT MODES:
      --json       Produce full JSON status output.

QUERY FLAGS:
  Extract individual fields for scripting. When any query flag is present,
  the command enters query mode: fields are printed to stdout (one per
  line, or space-separated with multiple flags), with no colored output
  or chatter on stderr.
  Computation is lazy — only requested fields incur git operations.

  -b, --branch     Current branch name (empty if detached).
  -r, --remote     URL of the tracking remote (empty if none).
  -u, --upstream   Upstream tracking branch name (empty if none).
  -H, --hash       Full commit hash (HEAD).
  -s, --short-hash Short commit hash (HEAD).
  -A, --ahead      Number of commits ahead of upstream.
  -B, --behind     Number of commits behind upstream.
  -C, --counts     Combined ahead/behind as "ahead behind".
  -I, --ignored    Count of ignored files.
  -K, --untracked  Count of untracked files.
  -S, --staged     Count of staged files.
  -U, --unstaged   Count of unstaged files.
      --conflicted Count of conflicted (unmerged) files.
      --ball       Ball emoji indicating repo state (see below).
  -z, --null       Separate output with NUL (\0) instead of newline.

  Query flags are mutually exclusive with --json.

DESCRIPTION:
  Empty output for -r means no upstream tracking branch is configured.

  Without flags, prints a colored one-line summary to stderr:

    ⚪ HEAD: abc1234 🌿main...origin/main [ahead 2] │ 📦 Staged: ... │ K:3 I:0

  The leading ball emoji encodes the most important state at a glance:

    🟢 Green   — staged changes only (ready to commit)
    🔴 Red     — conflict state (unmerged files), else unstaged changes only
    🟡 Yellow  — both staged and unstaged changes
    🟣 Purple  — untracked files only (no staged/unstaged)
    ⚫ Black   — ignored files only
    ⚪ White   — clean working tree

  In query mode, each flag outputs its corresponding field value to stdout,
  making it safe for pipes and scripting. Multiple flags output fields in
  canonical order, space-separated (or NUL-separated with -z).
  Canonical order: branch remote upstream hash short-hash ahead behind
  staged unstaged untracked ignored conflicted ball

CAPTURING OUTPUT:
  Default mode outputs to stderr (safe to capture the summary line).
  Query mode outputs to stdout with zero stderr chatter:
    branch=$(hug s -b)          # Capture branch name
    hug s -C -S -U              # "ahead behind staged unstaged"
    hug s --conflicted          # Count of conflicted (unmerged) files
    hug s -z -b -H | xargs -0  # NUL-separated for unusual names

EXAMPLES:
  hug s                         # Colored summary line (default mode)
  hug s --json                  # Full JSON status object
  hug s -b                      # Print branch name
  hug s -H                      # Print full commit hash
  hug s -b -H --ball            # Branch, hash, and state emoji
  hug s -C -K                   # Ahead/behind counts and untracked count
  hug s --conflicted            # Count of conflicted (unmerged) files
  hug s -z -b -H                # NUL-separated branch and hash

SEE ALSO:
  hug sl   : List file statuses with state labels (S:Mod, U:Del, etc.)
  hug sla  : Like sl, including untracked files
  hug slc  : List conflicted (unmerged) files only
  hug sw   : Show patch + file stats of all working directory changes
  hug ss   : Show patch + file stats of staged changes only
  hug sh   : Show commit details with file statistics output: xargs -0 -r present, bare xargs -0 absent), per the issue's own freshness directive. When the docs-page copy of the example was found, extended scope by one markdown line so both surfaces users copy from teach the guarded shape; committed as a separate docs commit (6f371bce) to keep the reviewed fix commit (f18d538f) stable, following the repo's docs-last multi-commit convention.
**Alternatives**: 1) Treating line 84 as live code and writing a sentinel-file zero-invocation test — rejected: the xargs call exists only in help text; such a test would test nothing real. 2) Amending the docs line into the fix commit — rejected: rewrites an already-reviewed SHA for a one-line addendum. 3) Leaving the docs page out of scope — rejected: the issue's stated intent is that users copy a SAFE pipeline, and the docs page is the most-read surface; leaving it bare would keep the hazard alive.
**Sources**: https://github.com/elifarley/hug-scm/issues/281, https://www.gnu.org/software/findutils/manual/html_node/find_html/xargs-options.html
**References**: elifarley/hug-scm#281
