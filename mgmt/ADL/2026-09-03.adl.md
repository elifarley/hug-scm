# ADL — Autonomous Decision Log — 2026-09-03

Autonomous agent decisions with reasoning and sources.
Scope: choices made without explicit user direction. User-driven -> DEC-NNN (/kanban-ops).

<!-- Entries: append below with UTC-3 timestamp -->

## 19:22 — Treat false pipe-safe teaching lines as in-scope stale sites for #280 despite zero literal pipes remaining

**Context**: Freshness check on origin/main 292bbb36 (v1.16.0.0) before executing elifarley/hug-scm#280: the mandatory grep found NO literal 'hug shc -n | <pipe>' shapes left in docs/skills/ — the ~200 commits since filing had rewritten the four known sites as bare 'hug shc -n' lines. Orchestration instructions said to STOP and report ALREADY_FIXED if nothing stale remained.
**Decision**: Classified the sites as STILL STALE and proceeded with the fix: the four lines still taught line mode as the machine-consumption form ('pipe-safe', 'for piping into other tools', 'scriptable pre-commit file list'), which hug shc -h itself contradicts (line mode C-quotes newline/tab/quote/backslash names; only -z is fully raw NUL-separated). A vacuously-met acceptance criterion does not make the guides safe — an agent reading 'pipe-safe' still pipes line mode and hits exit-123 failures. Fix applied: split display vs machine use at all four sites, teach 'hug shc -n -z | xargs -0 -r' with the GNU -r note, add a verified newline-filename round-trip example.
**Alternatives**: ALREADY_FIXED: report and stop, since the issue's literal acceptance text scopes to 'hug shc -n | <pipe>' shapes and none exist. Rejected: it would leave the exact falsehood (-z absent from docs/skills/, line mode endorsed for piping) live in the four guides the issue names, defeating the issue's stated intent.
**Sources**: https://github.com/elifarley/hug-scm/issues/280, https://github.com/elifarley/hug-scm/issues/274
**References**: elifarley/hug-scm#280

## 19:22 — Expand #280 fix to docs/cookbook.md and defer the git-shc help-text fix to a follow-up issue

**Context**: Code-quality review of the #280 docs branch found docs/cookbook.md carrying a fifth live 'line-mode shc -n is pipe-safe' claim (outside the issue's literal docs/skills/ acceptance path), and separately noted git-shc's own CAPTURING OUTPUT help section still carries one 'pipe-safe' example comment (code scope, not docs).
**Decision**: Fixed cookbook.md on this branch (commit 6cb2e194) — same falsehood, same failure mode, one-line class-of-fix; a docs PR that leaves a user-facing doc teaching the broken pipe shape fails the issue's intent even if the letter of the acceptance text is met. Deferred the git-shc help-text comment to a follow-up GitHub issue referenced from the PR body, keeping this PR docs-only.
**Alternatives**: Strict scope: leave cookbook.md for a separate issue — rejected as churn-amplifying (a second PR to delete one stale comment the review already diagnosed). Fix git-shc help text here too — rejected: turns a docs-only PR into a code change, dragging the full test matrix into CI for a comment tweak.
**Sources**: https://github.com/elifarley/hug-scm/issues/280
**References**: elifarley/hug-scm#280
