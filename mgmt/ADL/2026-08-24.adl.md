# ADL — Autonomous Decision Log — 2026-08-24

Autonomous agent decisions with reasoning and sources.
Scope: choices made without explicit user direction. User-driven -> DEC-NNN (/kanban-ops).

<!-- Entries: append below with UTC-3 timestamp -->

## 10:41 — PR 316 fix round: guard scan order + [[ ]] grouping

**Context**: Addressing two codex-connector P2 threads on the #313 guard-extraction implementation-plan PR (elifarley/hug-scm/pull/316).
**Decision**: Kept byte-order parity in guard_single_file_candidates: extras scanned FIRST, survivor LAST ('fa --bad1 --bad2' names --bad2), matching the four inline blocks verbatim; placed pathspecs_nonempty OUTSIDE [[ ]] via '{ [[ file||extras ]] || pathspecs_nonempty; }' (precedence-equivalent grouping); added pin rows for both previously-untested behaviors (two-flag precedence; bare 'h steps --browse-root').
**Alternatives**: Survivor-first scan (rejected: changes which token the error names - observable drift vs the byte-for-byte requirement); embedding the predicate inside [[ ]] (rejected: Bash reads it as a constant non-empty string, always truthy).
**Sources**: https://github.com/elifarley/hug-scm/pull/316
**References**: PR#316, issue elifarley/hug-scm#313

## 21:40 — PR 316 review-fix scope: Dependabot push-time alert

**Context**: While pushing the #3848518920 fix to PR 316, GitHub reported '8 vulnerabilities on elifarley/hug-scm default branch (3 high, 5 moderate)' via the push remote banner.
**Decision**: No new tracking issue created: the alerts live on main (not the PR branch), are pre-existing, and Dependabot itself is the canonical tracker (security tab surfaces them with remediation PRs). A duplicate manual issue would add noise without new information. Surfaced in the PR resolution report instead.
**Alternatives**: File a GitHub tracking issue per pr-fix's out-of-scope-item guidance; link it from the PR.
**Sources**: ⚠️ TODO — SOURCES MISSING (web-search guidance before deciding, then edit this entry)
**References**: elifarley/hug-scm#316
