.aid*

### Core Project Outputs ###
# Vite build output directory
dist/
.temp/
.cache/
cache/

# Local test dependencies
tests/deps/

# VHS binary (downloaded by vhs-build.sh)
docs/screencasts/bin/vhs

### Development Artifacts ###
# Vite dev server cache (critical for HMR performance)
.vite/
# npm dependencies (NEVER commit this)
node_modules/
# Local environment variables (keep .env.example!)
.env.local
# Environment-specific local files (e.g., .env.staging.local)
.env.*.local

### Testing & Linting ###
coverage/
# NYC test coverage artifacts
.nyc_output/
# Jest transformer cache
jest-cache/

### IDE & Editor ###
# User-specific VS Code settings (share .vscode/extensions.json instead!)
.vscode/**/settings.json
# ✅ Keep this to standardize dev tooling
!.vscode/extensions.json
# JetBrains IDE (WebStorm/IntelliJ)
.idea/
# Sublime Text
*.sublime-workspace
# Vim swap files
*.swp

### OS & Misc. ###
# macOS metadata
.DS_Store
# Windows thumbnail cache
Thumbs.db
# npm error logs
npm-debug.log*

### Documentation Images ###
# NOTE: VHS-generated screenshots (docs/commands/img/*.gif, *.png) 
# SHOULD be committed to ensure fast CI builds and consistent documentation.
# They are automatically regenerated via scheduled GitHub Actions workflow.
# See docs/VHS_CI_INTEGRATION.md for details.
/.claude/settings.local.json
/.hitm/received.txt
/.hitm/send.txt
/.claude/agent-responses/
