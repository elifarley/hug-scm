---
trigger: model_decision
description: when running commands to run tests, check code quality, lint or format code, or anything typical of a Makefile
---

Always prefer to run Makefile targets if you can find one that does what you need and is typical of a Makefile.
