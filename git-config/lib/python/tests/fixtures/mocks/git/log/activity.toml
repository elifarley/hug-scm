[metadata]
description = "Mock data for git log --follow (activity analysis)"
generated_by = "generate_mocks.py"

[[scenario]]
name = "burst"
description = "Burst pattern - many commits in short time"
command = [
    "git",
    "log",
    "--date=format:%Y-%m-%d %H:%M:%S %z",
    "--pretty=format:%ad|%an",
    "--follow",
    "--",
    "{filepath}",
]
returncode = 0
output_file = "outputs/activity_burst.txt"
stderr = ""

[[scenario]]
name = "weekend"
description = "Weekend work pattern - commits on Saturday/Sunday"
command = [
    "git",
    "log",
    "--date=format:%Y-%m-%d %H:%M:%S %z",
    "--pretty=format:%ad|%an",
    "--follow",
    "--",
    "{filepath}",
]
returncode = 0
output_file = "outputs/activity_weekend.txt"
stderr = ""

[[scenario]]
name = "empty"
description = "No commits for file"
command = [
    "git",
    "log",
    "--date=format:%Y-%m-%d %H:%M:%S %z",
    "--pretty=format:%ad|%an",
    "--follow",
    "--",
    "{filepath}",
]
returncode = 0
output_file = "outputs/activity_empty.txt"
stderr = ""
