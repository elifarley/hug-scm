#!/usr/bin/env bash
set -euo pipefail

# Enhanced status: shows latest commit hash + message, then status

git rev-parse --git-dir > /dev/null 2>&1 || { echo 'Not a git repository' >&2; exit 1; }
git -c color.ui=always status -sb "$@" | tail -n +2
git s
